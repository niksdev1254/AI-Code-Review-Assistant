{
  "name": "code-review-server",
  "version": "1.0.0",
  "description": "Backend server for code review application",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "PORT=3002 node --watch index.js",
    "start": "node index.js",
    "build": "echo 'No build step required for this Node.js server'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "@supabase/supabase-js": "^2.39.0",
    "@google/generative-ai": "^0.2.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0"
  }
}
